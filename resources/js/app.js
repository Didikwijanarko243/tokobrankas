import { initProductGallery } from './product-gallery';
import { initQuantitySelector, initWhatsappBuyButton } from './quantity-selector';
import { initPromoSlider } from './promo-slider';

document.addEventListener('DOMContentLoaded', () => {
    initProductGallery();
    initQuantitySelector();
    initWhatsappBuyButton();
     initPromoSlider();
});//
